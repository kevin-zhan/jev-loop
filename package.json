{
  "name": "pi-jev-loop",
  "version": "0.2.0",
  "description": "Managed Jev Loop runtime bridge for pi: leases, event journal, exclusive resources, async cognition",
  "license": "MIT",
  "repository": { "type": "git", "url": "git+https://github.com/kevin-zhan/jev-loop.git" },
  "homepage": "https://github.com/kevin-zhan/jev-loop#readme",
  "bugs": "https://github.com/kevin-zhan/jev-loop/issues",
  "keywords": [
    "pi-package",
    "ai-agents",
    "decision-runtime",
    "event-sourcing"
  ],
  "type": "module",
  "pi": {
    "extensions": [
      "./extensions/pi-jev.ts"
    ],
    "skills": [
      "./skills/pi-jev"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "scripts": {
    "test": "node --test tests/pi-extension/*.test.mjs"
  }
}
